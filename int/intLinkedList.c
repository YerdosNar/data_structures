#include <stdio.h>
#include <stdlib.h>

struct Node {
    int element;
    struct Node *next;
};

struct Node *arr;

void init_ll() {
    arr->next = NULL;
}

void add_ll(Node *arr, int element) {
    struct Node current = {element, NULL};

}
